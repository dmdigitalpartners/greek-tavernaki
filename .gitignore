# Dependencies
node_modules/

# Vercel
.vercel/

# Claude Code (local config — paths are machine-specific)
.claude/

# Claude Code temp files
temporary screenshots/

# OS files
.DS_Store
Thumbs.db

# Raw image originals — webp-only is served; PNGs kept out of git/deploy
menu-images/
Visual Assets/*.png
brand_assets/*.png
brand_assets/allergen-icons/*.png
*.MOV

# Environment variables (if ever added)
.env
.env.local
.env.*.local
.vercel
